<form class="babylon-sheet-config">
  <p class="hint" style="margin:0 0 .75rem 0; opacity:.85;">이 설정은 <b>현재 배우(캐릭터/보스)</b>에만 적용돼요. (배우별 저장)</p>

  <h3 style="margin:.25rem 0 .5rem 0;">시트 테마</h3>

  <div class="form-group">
    <label>테마</label>
    <div class="form-fields">
      <select name="theme">
        <option value="obsidian" {{#if (eq cfg.theme "obsidian")}}selected{{/if}}>오브시디언 (다크 기본)</option>
        <option value="azure" {{#if (eq cfg.theme "azure")}}selected{{/if}}>아주르 (차가운 블루)</option>
        <option value="crimson" {{#if (eq cfg.theme "crimson")}}selected{{/if}}>크림슨 (붉은 톤)</option>
        <option value="emerald" {{#if (eq cfg.theme "emerald")}}selected{{/if}}>에메랄드 (초록 톤)</option>
        <option value="parchment" {{#if (eq cfg.theme "parchment")}}selected{{/if}}>파치먼트 (라이트)</option>
      </select>
    </div>
    <p class="notes" style="margin:.25rem 0 0 0; opacity:.8;">배경/카드/탭의 톤을 바꿉니다. <b>RPG 바 컬러</b>와 별개예요.</p>
  </div>

  <hr/>

  <div class="form-group">
    <label>초상 크기</label>
    <div class="form-fields">
      <input type="number" name="portraitSize" value="{{cfg.portraitSize}}" min="160" max="180" step="5"/>
    </div>
  </div>

  <div class="form-group">
    <label>초상 표시</label>
    <div class="form-fields">
      <select name="portraitFit">
        <option value="cover" {{#if (eq cfg.portraitFit "cover")}}selected{{/if}}>자르기(cover)</option>
        <option value="contain" {{#if (eq cfg.portraitFit "contain")}}selected{{/if}}>맞춤(contain)</option>
      </select>
    </div>
  </div>

  <hr/>

  <div class="form-group">
    <label>배지(이미지)</label>
    <div class="form-fields">
      <input type="text" name="badgeImage" value="{{cfg.badgeImage}}" placeholder="예: systems/.../badge.webp 또는 이미지 URL"/>
      <button type="button" class="file-picker" data-pick="badgeImage" title="파일 선택"><i class="fas fa-folder-open"></i></button>
    </div>
    <p class="notes" style="margin:.25rem 0 0 0; opacity:.8;">좌측 상단에 작은 아이콘처럼 표시됩니다. (투명 PNG/WebP 권장)</p>
  </div>

  <div class="form-group">
    <label>배지 크기</label>
    <div class="form-fields">
      <input type="number" name="badgeSize" value="{{cfg.badgeSize}}" min="24" max="96" step="2"/>
    </div>
  </div>

  
  <h3 style="margin:.25rem 0 .5rem 0;">RPG 바 컬러</h3>

  <div class="form-group">
    <label>강조색</label>
    <div class="form-fields">
      <input type="color" name="colorAccent" value="{{cfg.colors.accent}}"/>
      <input type="text" name="colorAccentText" value="{{cfg.colors.accent}}" style="width:120px;" />
    </div>
  </div>

  <div class="form-group">
    <label>에너지</label>
    <div class="form-fields">
      <input type="color" name="colorEnergy" value="{{cfg.colors.energy}}"/>
      <input type="text" name="colorEnergyText" value="{{cfg.colors.energy}}" style="width:120px;" />
    </div>
  </div>

  <div class="form-group">
    <label>피격(빨강)</label>
    <div class="form-fields">
      <input type="color" name="colorDamage" value="{{cfg.colors.damage}}"/>
      <input type="text" name="colorDamageText" value="{{cfg.colors.damage}}" style="width:120px;" />
    </div>
  </div>

  <div class="form-group">
    <label>보호막(파랑)</label>
    <div class="form-fields">
      <input type="color" name="colorShield" value="{{cfg.colors.shield}}"/>
      <input type="text" name="colorShieldText" value="{{cfg.colors.shield}}" style="width:120px;" />
    </div>
  </div>

  <p class="hint" style="margin:.75rem 0 0 0; opacity:.85;">초상이 잘리면 ‘맞춤(contain)’으로 바꿔보세요.</p>
</form>
